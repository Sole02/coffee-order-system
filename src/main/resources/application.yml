spring:
  application:
    name: coffee-order-system
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

data-collection:
  base-url: http://localhost:8080